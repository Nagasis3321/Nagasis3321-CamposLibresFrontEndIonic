import { FilterOwnersPipe } from './filter-owners.pipe';

describe('FilterOwnersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOwnersPipe();
    expect(pipe).toBeTruthy();
  });
});
